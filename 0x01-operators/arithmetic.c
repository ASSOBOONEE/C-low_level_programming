#include <stdio.h>
	int main (void) {
	int x = 12, y = 4 ;

	printf ("x + y = %d\n", x + y);
	printf ("x - y = %d\n", x - y);
	printf ("x - y = %d\n", x * y);
	printf ("x / y = %d\n", x / y);
	return 0;
}
